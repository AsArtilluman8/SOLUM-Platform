name: Compile Filament Materials

on:
  workflow_dispatch:

permissions:
  contents: read

# The only workflow ID registered on the default branch. Patch 3 delegates this branch run to
# the exact Cardinal R23 build without merging the fork recipe into main first.
jobs:
  cardinal-r23:
    uses: ./.github/workflows/compile-filament-cardinal-r23.yml
