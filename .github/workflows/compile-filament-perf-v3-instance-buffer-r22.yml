name: Filament v1.71.4 SOLUM PERF V3 Native Instance Buffer R22

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

# This path is registered on the default branch. On the Patch 3 branch it delegates to the
# branch-owned Cardinal job, allowing a fork build before any main-branch merge.
jobs:
  cardinal-r23:
    uses: ./.github/workflows/compile-filament-cardinal-r23.yml
